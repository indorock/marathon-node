# DB_ADAPTER selects the backend: "sqlite" (default) or "mongodb"
DB_ADAPTER=sqlite

# SQLite — path to the .db file (default: <cwd>/db/marathon.db)
# DB_PATH=/absolute/path/to/marathon.db

# MongoDB — only used when DB_ADAPTER=mongodb
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=marathon
